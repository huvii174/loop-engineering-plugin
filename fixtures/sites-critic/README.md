# fixtures/sites-critic

A tiny app with a design-gate goal to route every price through one formatter
(`.loop/goal-*.md`, `.loop/design.md`, `.loop/prompt.md`). Used to exercise the
plan-critic. Never modified after its design gate (`SHA256SUMS`).
