# Design — route every price through formatPrice
## Work breakdown
1. Replace each `.toFixed(2)` price in `src/` with `formatPrice(...)` from `src/money.mjs`.
## Verification
The grep and the node one-liner in C1.
