# Epic: Demo totals
Two items: a CLI total, then JSON output.

## Epic acceptance criteria (measurable)
- [ ] AC1 — `node src/total.mjs` adds its arguments.
      Evidence: `node src/total.mjs 2 3` prints `5`.
