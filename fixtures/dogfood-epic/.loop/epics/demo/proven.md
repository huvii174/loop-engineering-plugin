# Proven criteria — demo

## Item 1
Source: item 1's goal.md at close

- [x] C1 the plain total
      Done when: `node src/total.mjs 2 3` prints `5` and exits 0
      Evidence: CLI output → the command itself
      Must not: a new dependency
