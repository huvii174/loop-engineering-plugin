# Backlog — Enforce "fix the class" and epic integration
| # | Sub-goal | Done when (seed) | Must not (seed) | Depends on | Epic criterion | Tier | Cond. | Status |
|---|----------|------------------|-----------------|------------|----------------|------|-------|--------|
| 0 | Baseline row: the two-writer fixture goes through the CURRENT verifier and recorder. Fixture (authored at this item's design gate as an anchor): two parallel writers with the same defect, a `goal.md` whose criterion is universal, a diff fixing only one writer, a payload that does not mention the other. Spawn the unmodified `loop-verifier`, then record its verdict with the unmodified `loop-record.mjs`. | The fixture's `.loop/state.json` `history[0]` has column `verdict` = `pass` and `criterion` = the universal criterion, written by `loop-record.mjs` after a real `loop-verifier` spawn (transcript kept). | Must not edit `agents/loop-verifier.md` or `scripts/loop-record.mjs`; the payload must not name the second writer; the fixture is hashed at the design gate and untouched afterwards. | — | AC1 (the baseline that goes red) | small | — | done (2026-09-23, 1 iteration, baseline 0/3 APPROVE) |
| 1 | Verifier check 9: an unaccounted `Sites:` hit is a REJECT. `Sites:` on the criterion shape and `Sweep:` on the iteration-record format get their one home in `skills/loop-engine/SKILL.md`; `commands/loop.md` payload gains the sweep block as a pointer; `agents/loop-verifier.md` check 9 re-runs the grep itself and skips on `Sites: none (<reason>)`. | On a hardened fixture authored at this item's design gate (sibling in another module, a different idiom, a large unrelated diff; D-eci-013), the current verifier is re-measured over 3 spawns first; then, with check 9, spawns return REJECT whose reason quotes the sibling's `Sites:` hit, the payload never naming it; `grep -rn "Sites:\|Sweep:" commands skills agents` shows one definition. | Must not alter checks 1–8 or the payload's existing sections; must not restate the contract outside loop-engine; must not touch the fixture. | 0 | AC1, AC5 | medium | row 0's verdict: if the current verifier already REJECTs the half-fix, this item shrinks to formalising the contract (say so on the rollup row). **Baseline (item 0): 0/3 APPROVE — 3/3 REJECTs name `litellm`** on the two-file fixture; the probe fired → formalisation, unless the fixture is hardened at this item's design gate | done (2026-09-23, 8 iterations incl. 4 review fixes; check 9 shipped in 0.18.1 + uncommitted review fixes; baselines 0/3, 0/3; half 3/3 REJECT, full 3/3 APPROVE) |
| 2 | `loop-record.mjs` refuses a missing `Sweep:`. Required when `--kind review-fix`, or when the `--criterion` targeted in `.loop/goal.md` carries `Sites:`; stderr names the fix. Cases in `scripts/test-loop-record.mjs`. | `node scripts/test-loop-record.mjs` exits 0 with the new cases and exits 1 when either new assertion is inverted. | Must not require `Sweep:` on a criterion without `Sites:` that is not review-fix; must not weaken any existing refusal; a refusal still writes nothing. | 1 | AC2 | small | — | done (2026-09-24, 5 iterations incl. 3 review fixes; 33 recorder cases) |
| 8 | `scripts/loop-close.mjs`: the per-item close is code. Lists the criteria to re-run (every `done` upstream item's refined `Done when:` + the epic AC the closing item claims), copying the closing item's refined criteria into `.loop/epics/<slug>/proven.md` so `archive/` stays frozen; accepts the verifier's verdicts; writes `done` on the backlog row and the verdict on the rollup row only when every verdict is APPROVE, else exits 1 and writes nothing. Tests in `scripts/test-loop-close.mjs`; dogfood fixture under `fixtures/`. | `node scripts/test-loop-close.mjs` exits 0, including the case where the fixture's item 2 breaks item 1's refined `Done when:` and close refuses; exits 1 when that refusal assertion is inverted. | Must not read `archive/` as authority; a refusal writes nothing; must not call a model (verdicts come in as input); must not touch `loop-record.mjs` refusals. | — | AC3 | medium | — | designed |
| 3 | Wire per-item close into the loop. `commands/breakdown.md` epic template: each AC carries `Done when:` / `Evidence:` (+ `Sites:` when universal); `commands/loop.md` "On every stop" step 3 replaces "tick any epic acceptance criteria now met" with: run `loop-close.mjs` to get the criteria to re-run, spawn `loop-verifier` (Template N) with them verbatim, hand the verdicts back to `loop-close.mjs`; a refusal routes back as a `review-fix` iteration in the same goal. | A fresh-context subagent given only the edited `commands/loop.md` and the dogfood fixture (item 2 breaks item 1) invokes `loop-close.mjs` and leaves item 2's backlog row not `done`, with the refusal on its rollup row; `grep` finds no remaining "tick any epic acceptance criteria" instruction. | Must not let the model tick an AC or write `done` itself; must not fan out reviewers here (mechanical re-run only); legacy `epic.md` without the shape stays readable; must not raise the item's `max_iterations`. | 8 | AC3 | medium | — | pending |
| 4 | `Sites:` authored at the design gate, refuted by the tenth man, and tier-rounded. `commands/design.md` goal template: a universal criterion gets `Sites:` (grep + hit list) or `Sites: none (<reason>)`; `agents/plan-critic.md` attack 8 re-runs every `Sites:` grep (hit missing from the list, or an opt-out reason it can refute → REVISE); loop-engine tier table: `Sites:` spanning ≥2 modules or a universal `Done when:` rounds to ≥ `medium`, applied by `agents/epic-planner.md`. | `plan-critic` spawned against a fixture `goal.md` whose `Sites:` list omits one grep hit returns REVISE quoting that hit; the same fixture with `Sites: none (<reason>)` gets the reason attacked in the transcript. | Must not change attacks 1–7; must not re-tier `trivial`/`small` goals without `Sites:`; the rule text lives once (loop-engine), design.md and plan-critic point. | 1 | AC1, AC5 | medium | — | pending |
| 5 | Epic review gate at last-item close, findings → an executed `integration` row. `skills/loop-review/SKILL.md` gains the Epic gate section (cross-item charter: sibling sites unfixed, logic duplicated across items drifting, epic invariants, spec fidelity against `epic.md`), recorded via `loop-record.mjs --review-gate`; `loop-close.mjs` gains the append: confirmed findings become one backlog row labelled `integration` (tier by finding count: 1–2 small, 3–5 medium, >5 large) and its id is added to `run.json.order`, so run-gate holds the session for it before the retro (owns the `runStanding` obstacle, and the `itemId` row-0 bug in `hooks/lib.mjs` — L-007, D-eci-014: run-gate must see row `0`); `commands/run.md` step 4 and `commands/loop.md` step 3 point at it. | `node scripts/test-loop-close.mjs` covers the append on the dogfood fixture (planted sibling site): the backlog gains an `integration` row, `run.json.order` contains its id, and `runStanding` from `hooks/lib.mjs` reports it as the next item; red when the append is removed. | Must not re-run per-goal dimensions on the accumulated diff; findings land nowhere but the backlog row; charter lives once (loop-review); must not bypass verifier/record/breaker for the row. | 2, 3 | AC4, AC5 | large | — | pending |
| 6 | Integration point: the same gate at the first convergence item. `agents/epic-planner.md` marks the first item where ≥2 independent chains converge (or the first end-to-end item) as `integration point`; `commands/loop.md` step 3 runs item 5's gate there with the full charter (assumption 2). At most two gates per epic (D-eci-005). | Dogfood epic of 3 items (item 3's anchor grown once more) with item 2 marked `integration point`: item 2's `state.json` carries a `review_gate` record before item 3's design gate starts. | Must not duplicate the charter (pointer to loop-review); must not gate every item; parallel merge-back stays the implicit point. | 5 | AC4 | medium | — | pending |
| 7 | Ship the epic's release. Finalise the one `CHANGELOG.md` entry opened by item 1 (drop "in progress") at whatever patch `.claude-plugin/plugin.json` carries then (0.18.1 as of item 1; D-eci-017, D-eci-018); `README.md` "What ships" if any file was added; writing-docs pruning pass on every touched doc; AC5 grep. | `node scripts/test-*.mjs` all exit 0, `loop-breaker.mjs` echoes the plugin version matching the CHANGELOG head, and `grep -rn "Sites:\|Sweep:" commands skills agents` shows one definition. | Must not bump without the CHANGELOG line; must not leave a touched doc without the pruning pass; one release only — an `integration` row landing after this item amends the epic's entry, never adds a second. | 4, 5, 6 | AC5 | small | — | pending |
